CREATE TABLE EMSD_CUERPO_BOLETA(
    COD_CUERPO NUMBER,
    DESCRIPCION_SERVICIO VARCHAR2(250),
    DESCRIPCION_REPUESTOS VARCHAR2(250),
    COSTO_TOTAL NUMBER,
    CONSTRAINT PK_EMSD_CUERPO_BOLETA PRIMARY KEY(COD_CUERPO)
);

CREATE OR REPLACE FUNCTION COSTO_TOTAL_VENTA(}
)
RETURN NUMBER
IS 
BEGIN


    COMMIT;
    EXCEPTION

    ROLLBACK;
END;
