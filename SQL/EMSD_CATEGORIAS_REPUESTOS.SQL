CREATE TABLE EMSD_CATEGORIAS_REPUESTOS(
    COD_CATEGORIA NUMBER,
    NOMBRE_CATEGORIA VARCHAR2(50),
    PK_EMSD_CATEGORIAS_REPUESTOS PRIMARY KEY(COD_CATEGORIA)
);

CREATE OR REPLACE TRIGGER EMSD_CATEGORIAS_REPUESTOS
BEFORE INSERT
ON EMSD_CATEGORIAS_REPUESTOS
FOR EACH ROW
BEGIN
	
END;


CREATE OR REPLACE PROCEDURE EMSD_CATEGORIAS_REPUESTOS_(


)
IS 
BEGIN
	LOCK TABLE EMSD_CATEGORIAS_REPUESTOS IN ROW EXCLUSIVE MODE;

	COMMIT;
	EXCEPTION
		WHEN NO_DATA_FOUND THEN
    		RAISE_APPLICATION_ERROR(+100, 'No existe');
    	WHEN STORAGE_ERROR THEN
            RAISE_APPLICATION_ERROR(-6500, 'Fallo de memoria');
    ROLLBACK;
END;