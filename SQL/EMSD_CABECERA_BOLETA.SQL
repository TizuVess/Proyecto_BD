CREATE TABLE EMSD_CABECERA_BOLETA(
    COD_CABECERA NUMBER,
    COD_CLIENTE NUMBER,
    FECHA_EMISION DATE,
    CONSTRAINT PK_EMSD_CABECERA_BOLETA PRIMARY KEY(COD_CABECERA)
);

CREATE OR REPLACE TRIGGER TR_NUEVA_VENTA
AFTER INSERT
ON EMSD_VENTA
FOR EACH ROW
BEGIN
